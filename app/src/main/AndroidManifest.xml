<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FloraliaProject"
        tools:targetApi="31">
        <activity
            android:name=".DetailActivityFlower"
            android:exported="false" />
        <!-- LoginActivity sebagai entry point aplikasi -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- MainActivity untuk halaman utama setelah login -->
        <activity
            android:name=".MainActivity"
            android:exported="false" /> <!-- RegisterActivity untuk registrasi pengguna -->
        <activity
            android:name=".RegisterActivity"
            android:exported="false" /> <!-- ContentActivity untuk menampilkan konten lain (jika ada) -->
        <activity
            android:name=".ContentActivity"
            android:exported="false" />
    </application>

</manifest>